#!/bin/sh
#
# Start birch and connect to #kisslinux irc channel.
#
# Usage: irc [password], irc [password] [username]

[ "$1" ] || exit 1

birch -u "${2:=$USER}" -c "#kisslinux" -x "/m nickserv identify $1"
