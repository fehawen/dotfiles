#!/usr/bin/env bash
#
# Generate colorscheme with wal and set wallpaper with feh
#
# Usage: luv, luv [image directory path, image file path]

arg=${1:-"$HOME/pictures/wallpapers"}

wal -n -i "$arg"
feh --bg-fill "$(< "${HOME}/.cache/wal/wal")"
